# Базовий образ
FROM node:20

# Встановлюємо робочу директорію
WORKDIR /app

# Копіюємо файли залежностей
COPY package*.json ./

# Встановлюємо залежності (включно з dev-залежностями)
RUN npm install

# Копіюємо весь проєкт
COPY . .

# Вказуємо порт 
EXPOSE 3000

# Запускаємо розробницький сервер
CMD ["npm", "start"]


